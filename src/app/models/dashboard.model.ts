export interface WidgetType {
  id: string;
  label: string;
  rows: string;
  columns: string;
  backgroundColor: string;
  color: string;
  progress: number;
  interaction: number;
  iconType: string;
}
